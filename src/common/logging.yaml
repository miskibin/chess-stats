version: 1
disable_existing_loggers: True
formatters:
  chess_format:
    format: "%(asctime)s ::%(levelname)-8s :: %(filename)-20s ::%(message)s"

handlers:
  console_handler:
    class: logging.StreamHandler
    level: DEBUG
    formatter: chess_format
    stream: ext://sys.stdout

  file_handler:
    formatter: chess_format
    level: DEBUG
    name: null
    path: "./logs"
    (): src.common.logging_config_utils.set_log_file_path

loggers:
  chess_logger:
    handlers: ["console_handler", "file_handler"]
